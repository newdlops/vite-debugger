import { defineConfig } from 'vite';

// A tiny marker config so the extension recognizes this folder as a Vite
// project (it greps for vite.config.* in the workspace to decide whether to
// surface its status bar item).
export default defineConfig({});
